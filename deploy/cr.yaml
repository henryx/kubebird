apiVersion: kubebird.github.io/v1
kind: Instance
metadata:
  name: test
spec:
  image: firebirdsql/firebird
  version: 3.0.14
  databases:
    - name: "instance.fdb"
      shadow: false
      pageSize: 8192 # can be 4096, 8192, 16384 or 32768. default is 8192
      charset: "UTF8"
      collation: "UTF8"
    - name: "shadowed.fdb"
      alias: "enforced" # if not specified, uses database name as alias
      shadow: true
  service:
    type: ClusterIP
    port: 3050 # port the Service exposes the instance on. Default is 3050
  storage:
    primary:
      class: "" # if empty, uses the default storage class
      size: 3Gi
    shadow: # can be omitted if no database above has "shadow: true"
      class: ""
      size: 3Gi
    backup: # can be omitted if we want to use S3 storage for backups
      class: ""
      size: 3Gi
  authentication:
    sysdba:
      secretRef: ""
